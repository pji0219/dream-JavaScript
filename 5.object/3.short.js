const x = 0;
const y = 0;
// 객체 축약
const coordinate = { x, y }; //{ x: x, y: y };
console.log(coordinate);

function makeObj(name, age) {
  return {
    name,
    age,
  };
}

console.log('pji', 34);
